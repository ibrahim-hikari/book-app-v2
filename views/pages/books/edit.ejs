<!DOCTYPE html>
<html lang="en">

<head>
    <title>Add This Book</title>
    <link rel="stylesheet" href="css/add.css">
</head>

<body>
    <main>
        <div class="fog"></div>
        <form action="/update/<%= book.id%>" method="post">
            <label>
                <input type="hidden" name="_method" value="put">
                <input type="hidden" name="id" value="book.id">
                <input type="text" name="title" placeholder="Populated Title:" value="<%= book.title%>">
                <input type="text" name="author" placeholder="Populated Author:" value=" <%= book.author%>">
                <input type="text" name="isbn" placeholder="Populated ISBN:" value="<%= book.isbn%>">
                <input type="text" name="image_url" value="<%= book.image_url%>">
                <textarea name="description" placeholder="Populated Description:">"<%= book.description%>"</textarea>
                <input type="text" name="bookshelf" placeholder="Enter a Bookshelf Name:" value="<%= book.bookshelf%>">
                <div class="submit">
                    <input type="submit" value="Update Book"></input>
                </div>
            </label>
        </form>
    </main>
</body>

</html>